<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <ImageView
        android:id="@+id/image"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:src="@drawable/icon_gesture1"
        android:scaleType="fitCenter"
        />
    <TextView
        android:id="@+id/text"
        style="@style/font_regular"
        android:layout_marginTop="@dimen/margin_extra_thin"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/popup_gesture_text0"
        android:textAlignment="center"
        android:textSize="@dimen/font_extra_bold"
        android:textColor="@color/colorYellowExtraLight"
        />
</LinearLayout>